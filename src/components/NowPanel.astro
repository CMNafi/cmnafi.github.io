---
interface Props {
  title: string;
  items: string[];
}

const { title, items } = Astro.props;
---

<section class="now-panel panel">
  <h3>{title}</h3>
  <ul>
    {items.map((item) => <li>{item}</li>)}
  </ul>
</section>
