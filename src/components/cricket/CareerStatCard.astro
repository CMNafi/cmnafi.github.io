---
interface Props {
  label: string;
  value: string;
  detail: string;
}

const { label, value, detail } = Astro.props;
---

<article class="panel career-stat-card">
  <span>{label}</span>
  <strong>{value}</strong>
  <p>{detail}</p>
</article>
